[tool.ruff]
line-length = 160
target-version = "py312"
extend-exclude = ["migrations/versions"]

[tool.ruff.lint]
# Begin with correctness, import, upgrade, and common bug checks that are useful in CI.
select = ["E4", "E7", "E9", "F", "I", "UP", "B"]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["B011"]
