# MAblog build progress

Last updated: 2026-09-24.

Project: `F:\AI_Roadmap_2026\MAblog`.

Current state: the local multi-user blog, permission-aware search/RAG release, animated Discover design, and weekly `MABlog_IA` newsroom are implemented. The newsroom has a protected bilingual administrator workspace, durable PostgreSQL stages, official-source evidence, strict fetching, Luna/Terra generation, safe bilingual publication, corrections, monitoring, retention, and separate Docker worker. Ordinary administrator previews omit claim fact-checking but remain private until guarded human approval; the automatic and activation-test paths retain verification. The most recent isolated backend suite passes 48 tests; frontend TypeScript, lint, optimized build, function-comment checks, and all 27 browser workflows pass. An earlier real 30-day historical private preview completed every stage at a measured estimated cost of USD 0.225456, produced a verified three-source bilingual edition, created no public post, and recorded the activation proof. The schedule remains disabled for administrator choice. Preparation entries retain their historical wording.

The weekly `MABlog_IA` blueprint was confirmed on 2026-09-14. Milestones I33-I44 are complete for local Docker operation; production deployment remains a future phase.

## How this log is maintained

Completed entries use checked boxes. Pending, in-progress, or blocked entries remain unchecked and state their status explicitly. Each delivered step gets a short-to-medium description, affected files, and the actual verification result. Future implementation will expand the milestones below into concrete steps as work is delivered.

The project-level `AGENTS.md` requires this log to be updated and requires documentation for every project function, plus comments explaining difficult logic and hard-coded business values. Preparation entries describe the state when they were recorded; subsequent implementation entries supersede them.

## Completed preparation

- [x] **P01 - Confirm project location and inspect local tools.** Selected the existing `F:\AI_Roadmap_2026\MAblog` directory and checked the tools needed for local development. The inspection on 2026-09-06 found Docker CLI and Compose installed, but the Docker Linux daemon was unreachable; its availability must be checked again before running the application. Node.js, npm, Git, and Python were available.

  **Record:** `docs/design-interview.md`, Environment facts. **Verification:** recorded filesystem and tool inspection; this does not establish that containers or application services run.

- [x] **P02 - Record product requirements and user decisions.** Captured profiles, public and personal posts, per-post email access, creator authority, drafts, approvals, carousel ranking, authentication, languages, local storage, and later discovery. Recorded the freeform canvas requirements, including mixed media, movement, rotation, overlap, scrolling, cropping without deletion, and separate layout review.

  **Record:** `docs/design-interview.md`. **Verification:** reviewed the recorded requirements against the interview; unresolved recommendations remain identified as proposals.

- [x] **P03 - Document the domain vocabulary.** Created a shared glossary for posts, authors, access grants, content blocks, drafts, review, discovery results, and canvas layout changes. This gives backend and frontend work consistent terms for the same product behavior.

  **Record:** `CONTEXT.md`. **Verification:** documentation review only; terminology has not yet been implemented in application models.

- [x] **P04 - Record collaboration and canvas decisions.** Saved the creator-controlled collaboration rules and the decision to use a freeform canvas. The records cover independent block approval, rejection of competing alternatives, retention of unrelated pending changes, and separate creator review of canvas resizing.

  **Records:** `docs/adr/0001-creator-controlled-post-collaboration.md` and `docs/adr/0002-freeform-post-canvas.md`. **Verification:** documentation review against the accepted interview decisions.

- [x] **P05 - Prepare the consolidated design review.** Combined agreed behavior, initial pages, proposed implementation defaults, review examples, and required validation into one review document. Preparing this document is complete; accepting its remaining proposals is a separate pending decision.

  **Record:** `docs/design-review.md`. **Verification:** document content inspected; synchronized project and conversation-output copies checked for matching file hashes.

- [x] **P06 - Record frontend libraries and separate application folders.** Added Next.js with shadcn/ui and Tailwind CSS, plus Zustand for later state management. Recorded separate top-level `frontend/` and `backend/` folders, with independent dependency/configuration files and Dockerfiles planned to support later deployment.

  **Records:** `docs/design-review.md` and `docs/design-interview.md`. **Verification:** requested wording checked and saved copies compared. These folders and applications have not been scaffolded yet.

- [x] **P07 - Establish progress tracking and code documentation rules.** Added this log and persistent project instructions requiring a short-to-medium explanation for completed build steps. The instructions require documentation for every project function and nearby explanations of complex logic and hard-coded business values; the requirement is also recorded in the design documents.

  **Records:** `AGENTS.md`, `docs/build-progress.md`, `docs/design-review.md`, and `docs/design-interview.md`. **Verification:** documentation consistency and file-copy checks. Function-comment coverage will be reviewed during implementation, when source code exists.

- [x] **P08 - Complete the weekly AI-news design and implementation blueprint.** Recorded the full one-question-at-a-time decision tree for the system-owned `MABlog_IA` publisher, then mapped the accepted behavior to additive PostgreSQL records, a dedicated Docker worker, focused FastAPI services, separated Next.js administrator components, security boundaries, rollout milestones I33-I44, and the real-preview activation contract. No application source or database migration was changed during this preparation step.

  **Records:** `docs/ai-news-publisher-design.md`, `docs/ai-news-publisher-implementation-plan.md`, `docs/adr/0003-weekly-ai-news-publisher.md`, and `CONTEXT.md`. **Verification:** inspected the existing Compose services, ORM records, authentication bootstrap, API composition, frontend routing, and progress history; reviewed the saved blueprint against all confirmed interview answers. Final user confirmation remains pending.

## Completed local build milestones

These milestones are complete. Implementation entries I01–I10 below record affected areas and verification.

- [x] **B01 - Scaffold the two applications and local services.** Completed; see I01–I10 below for affected areas and verification.
- [x] **B02 - Implement persistent models and migrations.** Completed; see I01–I10 below for affected areas and verification.
- [x] **B03 - Implement authentication and local email verification.** Completed; see I01–I10 below for affected areas and verification.
- [x] **B04 - Implement posts, profiles, permissions, and media.** Completed; see I01–I10 below for affected areas and verification.
- [x] **B05 - Implement working drafts and creator review.** Completed; see I01–I10 below for affected areas and verification.
- [x] **B06 - Build the visual interface and localization.** Completed; see I01–I10 below for affected areas and verification.
- [x] **B07 - Build the freeform composer and reader.** Completed; see I01–I10 below for affected areas and verification.
- [x] **B08 - Implement likes, the carousel, and selected caches.** Completed; see I01–I10 below for affected areas and verification.
- [x] **B09 - Complete integrated validation and fixes.** Completed; see I01–I10 below for affected areas and verification.
- [x] **B10 - Deliver local operation instructions.** Completed; see I01–I10 below for affected areas and verification.

## Later work

- Re-run `python -m app.evaluate_search` after changing the embedding model or retrieval configuration.
- OCR, image understanding, and uploaded-video/audio transcription remain outside the confirmed first search release.
- Production deployment, real email delivery, HTTPS, proxy identity handling, and production secret rotation will be discussed later.

## Implementation entries - 2026-09-07

- [x] **I01 - Scaffold independent applications and Compose services.** Created `frontend/` and `backend/` with their own dependency manifests, Dockerfiles, and application configuration. Compose runs Next.js, FastAPI, PostgreSQL, Redis, and Mailpit with database, uploads, and inbox volumes. **Verification:** Compose configuration validated; both Docker images built; all five services running; `/api/health` reports database and Redis healthy.
- [x] **I02 - Implement database models, authentication, and permission checks.** Added versioned schema setup, password hashing, opaque sessions, local email codes, seven-day expiry, creator authority, per-post grants, and protected uploads. Database-backed throttling remains available when Redis is down. **Verification:** PostgreSQL integration tests cover activation, code replay, weekly expiry, access roles, unknown email rejection, and private media.
- [x] **I03 - Implement drafts, independent approvals, and public ranking.** Added private working copies, per-target revision checks, competing-proposal rejection, separately reviewed canvas bounds, and timestamped likes. Carousel caching stores IDs and rechecks current publication state. **Verification:** seven integration tests passed, including revocation, unrelated pending changes, unpublishing, rolling-window likes, and Redis fallback.
- [x] **I04 - Fix deterministic block ordering.** Integration testing exposed that applying targets in an unordered set could return blocks in an unstable order. Normalized documents by logical reading order and stable block identity without altering geometry or stacking. **Verification:** reran the PostgreSQL suite; all seven tests passed.
- [x] **I05 - Build and verify browser workflows.** Implemented the bilingual shell, profiles, workspace, sharing, review, accounts, reader, and freeform editor. **Verification:** four Playwright tests passed, including actual email verification, drawing/dragging/resizing, rotation, crop retention, session recovery, carousel controls, phone layout, and two-account approval; TypeScript and Docker builds passed.
- [x] **I06 - Add original sample artwork and delivery instructions.** Added five original fantasy illustrations and an explicit idempotent seed command. **Areas:** `backend/seed-assets/`, `backend/app/seed.py`, `README.md`, `docs/local-operations.md`. **Verification:** seeded collection and carousel inspected at desktop and phone widths.
- [x] **I07 - Protect unsubmitted editor media.** Kept private draft uploads hidden from the creator until submission while preserving submitted media for review after revocation. **Areas:** `backend/app/main.py`, `backend/tests/test_workflows.py`. **Verification:** expanded PostgreSQL suite passed all ten tests, including stale review and authentication edge cases.
- [x] **I08 - Freeze migrations and preserve editor input.** Replaced live-model migration setup with fixed Alembic operations, localized API errors, preserved edits during cover upload, and added a focus-trapped reauthentication dialog. **Verification:** fresh database reached initial revision with eleven tables including Alembic; typecheck and browser workflows passed.
- [x] **I09 - Verify persistence and backup recovery.** Recreated the five containers and preserved five public posts and two captured emails. Ran the backup script and restored into an isolated database; compared the extracted media archive against stored uploads. **Areas:** `scripts/backup.ps1`, `docs/local-operations.md`, `backups/delivery-check/`. **Verification:** restored five posts/five media records and matching upload bytes.
- [x] **I10 - Audit function documentation.** Added purpose comments/docstrings to project functions, callbacks, handlers, and complex logic. **Verification:** Python and TypeScript AST audits reported no undocumented project functions; generated dependencies were excluded.
- [x] **I11 - Add collection category filtering.** Added a vertical bilingual menu with All posts, Technology, Travel, General, and Anime, and a selector in reviewed post details. Existing posts default to General; PostgreSQL filtering applies to approved public metadata before pagination without exposing private drafts. **Areas:** `backend/app/core.py`, `backend/app/main.py`, shared frontend category labels, composer, collection layout, tests, README, design review. **Verification:** eleven PostgreSQL tests passed; all four Playwright workflows passed in 28 seconds, including category save/filter, empty results, Spanish labels, and phone width; TypeScript and Docker builds passed, with desktop/phone screenshots inspected.
- [x] **I12 - Fix accessibility and close release records.** Browser testing exposed ambiguous exact label lookup for the category selector and overflow in the signed-in Spanish phone header. Added an explicit localized accessible name and allowed header controls to wrap. Refreshed stale milestone statuses, preserved earlier verification history, and removed the temporary public fixture left by the failed browser run. **Verification:** the complete browser suite passed after these fixes; the health endpoint reports PostgreSQL and Redis healthy. Function-comment audit returned no missing frontend function comments.
- [x] **I13 - Add a default local test administrator.** Added an opt-in, environment-configured account bootstrap after migrations. The account receives a long local verification window, so login requires only its username/email and password; regular accounts keep weekly email verification. It remains a normal content owner without cross-account access, and startup refuses to alter conflicting accounts. **Areas:** `backend/app/bootstrap.py`, backend startup, Compose and environment examples, PostgreSQL/browser tests, README, and design review. **Verification:** the rebuilt backend created the account in the persistent database and its live login returned a session without a verification challenge. All twelve PostgreSQL tests and five Playwright workflows passed; typecheck passed and all five services remained running.
- [x] **I14 - Stabilize collection card layout.** Scoped cover image rules so they cannot enlarge uploaded avatars, fixed avatar dimensions and circular cropping, clamped title/summary rows, and anchored author/read actions at the card bottom. Long author names truncate inside their available space. **Areas:** `frontend/src/components/posts.tsx`, collection CSS, browser regression check, and design review. **Verification:** TypeScript and the production frontend build passed; all six Playwright workflows passed in 26 seconds. A dedicated regression measured a 32 × 32 pixel uploaded avatar and aligned first-row title, summary, and footer positions. The reported wide layout and phone layout were captured and visually inspected.
- [x] **I15 - Make the page canvas directly resizable.** Enabled drag resizing from every canvas edge and corner during normal editing, added visible handles and bilingual guidance, and retained crop-area movement as a separate mode. Width/height fields update from the dragged geometry, and left/top resizing preserves document coordinates. **Areas:** `frontend/src/components/composer.tsx`, editor styles, browser test, README, and design review. **Verification:** TypeScript, function-comment audit, and the production frontend build passed. A browser dragged the visible top-left corner by 80 × 60 pixels and verified both dimensions updated; the complete six-workflow suite passed in 25.9 seconds. The live editor was captured and visually inspected with the user's existing post.
- [x] **I16 - Add quick block deletion.** Added a bilingual trash control to every block header and routed both header/sidebar removal through one history-aware helper. Pointer and mouse events stop before reaching the drag handle, so deletion does not move the block; Undo restores the complete block. **Areas:** `frontend/src/components/composer.tsx`, editor styles, browser test, README, and design review. **Verification:** TypeScript, function-comment audit, and the production frontend build passed. The authoring test deleted Block 2 from its header, verified only one block remained, used Undo, and verified both the block count and content were restored. The complete six-workflow browser suite passed in 26 seconds, and the live editor was visually inspected.
- [x] **I17 - Expand the composer with a settings dropdown.** Replaced the permanent right inspector column with a bilingual dropdown at the upper-right of the canvas. The closed menu gives the workspace full editor width; the opened overlay keeps all post, canvas, selected-block, layer, and draft controls in a bounded scroll area. **Areas:** composer component and responsive styles, browser regression, README, and design review. **Verification:** TypeScript, the function-comment audit, and the optimized frontend build passed. A desktop browser verified the panel begins closed, opens with all controls available, and leaves a viewport wider than 900 pixels at the suite's 1280-pixel screen size. All six browser workflows passed in 29.2 seconds, and the open overlay was captured and visually inspected against the existing administrator post.
- [x] **I18 - Refactor into production-style feature modules.** Replaced the large frontend aggregators with an application shell, eight feature folders, individual post/composer components, reusable lifecycle hooks, a separated API client, and focused style files. Split FastAPI into five route groups plus configuration, database, ORM model, Pydantic schema, permission, document, post, and cache modules; `main.py` now contains only application composition, middleware, and global error translation. Added permanent ownership rules and a complete module map so future search/RAG work follows the same boundaries. **Areas:** `frontend/src/app-shell`, `features`, `hooks`, `lib/api`, `styles`; `backend/app/api`, `services`, configuration/models/schemas; tests, AGENTS, README, and architecture documentation. **Verification:** TypeScript, Python compilation, function-comment audit, and optimized Docker frontend/backend builds passed. All twelve isolated PostgreSQL tests passed in the final 4.52-second run and all six Playwright workflows passed in 27.0 seconds after the restructure.
- [x] **I19 - Add durable approved-content search indexing.** Backed up the pre-RAG PostgreSQL and upload state, changed the local PostgreSQL 17 image to pgvector, and applied additive migrations for account consent, full-text/vector passages, durable latest-revision jobs, and one-use explanation-token digests. Approved post changes update keyword passages immediately; a separate worker embeds the current revision, reclaims abandoned leases, enforces its own daily allowance, retries on the accepted schedule, and exposes administrator prepare/retry commands. Drafts and proposal records are never indexed separately. **Areas:** Compose, migrations `0002`/`0003`, models, indexing/capacity/embedding services, worker, backup script, and `backups/pre-rag-pgvector-20260908/`. **Verification:** migration head `0003`; pgvector 0.8.6 enabled; eleven posts preserved; 23 keyword passages present; stale/retry/opt-out tests pass.

- [x] **I20 - Implement permission-aware hybrid retrieval and grounded generation.** Added PostgreSQL full-text and vector candidates with visibility, scope, category, publication, grant, and two-sided personal consent inside candidate SQL. Per-post Reciprocal Rank Fusion applies bounded 14-day-like and freshness boosts. Search returns Personal first and Public second, signs independent continuation pages, and rechecks mutable permissions before pagination and before loading answer evidence. Responses API streaming uses only retrieved MAblog passages, disables provider response storage, sends trusted numeric citation metadata separately, and preserves ranked keyword results across provider or allowance failures. **Areas:** search API, retrieval/generation/token services, configuration, schemas, profile consent endpoint, post approval/publication synchronization. **Verification:** tests cover anonymous/author/viewer/stranger visibility, draft/proposal exclusion, vector consent, revocation/publication rechecks, pagination, category changes, citations, replay blocking, provider failure, privacy-safe logs, and layered rate limits.

- [x] **I21 - Deliver the bilingual search experience.** Added a global English/Spanish search control and dedicated explanation-first page using transient Zustand state. The page provides a 500-character natural-language form, category and Personal/Public scope filters, immediate ranked groups, independent Load more actions, streamed explanation states, creator indexing status, and reader deep links that scroll to and highlight the matched block. Profile settings explain cloud processing and let users opt personal posts in or out. Search files remain separated by component, hook, store, API transport, and focused stylesheet. **Areas:** `frontend/src/features/search/`, API transport types/client, profile/workspace/post components, reader, application shell, and search styles. **Verification:** TypeScript passes; the browser search workflow verifies header navigation without URL query persistence, public ranking, explanation fallback, matched-block highlighting, Spanish labels, and 390-pixel layout.

- [x] **I22 - Close search quality and local operations.** Added a 16-case synthetic retrieval fixture and repeatable live evaluator with the accepted 85% overall and 80% bilingual top-five gates; deterministic integration covers both English-to-Spanish and Spanish-to-English retrieval. Moved Playwright binaries/temp data to the project drive, repaired collaboration test session isolation, normalized copied invitation emails, documented all search configuration and recovery commands, updated backup/restore to pause the worker, and added a reusable TypeScript callback-comment audit. **Areas:** backend evaluation/test files, Playwright wrapper/tests, invitation endpoint, environment example, README, architecture/domain/RAG/operations documents. **Verification:** final production Compose build passes; 27 backend tests pass in 6.98 seconds; all seven browser workflows pass in 27.3 seconds; TypeScript, Python compilation, and both function documentation audits pass; all six services are running with healthy PostgreSQL, Redis, and API. The live evaluator reached OpenAI and returned `embedding_http_429`, so no live model quality score is claimed; keyword fallback and pending durable jobs were verified instead.

- [x] **I23 - Repair title matching and compact the search form.** Changed lexical lookup to safe prefix lexemes so approved title metadata matches common suffix variants such as `mountain` and `mountains`. Reduced the question field to one compact row while keeping a controlled resize range. When embedding creation fails after enhanced-search capacity is granted, permission-checked lexical evidence can still drive the streamed explanation; an answer-provider failure continues to leave ranked results visible. Diagnosed the configured OpenAI project response as `429 insufficient_quota / credit_balance_exhausted`; six durable jobs remain pending and will retry without rebuilding content after capacity returns. **Areas:** retrieval and search API services, search form/styles, backend/browser regressions, README, and local operations. **Verification:** production backend/frontend images build; all 28 isolated backend tests pass in 6.86 seconds; TypeScript and the callback-comment audit pass; all seven Playwright workflows pass, including a 40-pixel query field and the exact `mountain` to `Where the mountains remember` result. The provider-outage regression also streams an explanation from approved lexical evidence. The live API returns that public title in keyword mode, database/Redis health is true, and all six Compose services are running.

- [x] **I24 - Stabilize layout during navbar navigation.** Reserved the root scrollbar lane so centered content keeps the same horizontal origin when destinations have different heights. Reserved the active underline border on every navbar link and applied the active state to My atelier, preventing the navigation baseline from changing when that route opens. **Areas:** shared base styles, application shell, browser regression, and this progress record. **Verification:** TypeScript and the callback-comment audit pass; the optimized frontend image builds and is running. A dedicated Playwright workflow measured the brand, main page container, and first navbar item across Discover, The collection, and My atelier, with every coordinate changing by less than 0.5 pixels. All eight browser workflows pass in 30.8 seconds.

- [x] **I25 - Keep the canvas frame at its dropped position.** Replaced the canvas frame's hard-coded controlled position with explicit workspace state. Dragging or resizing now applies the same delta to the visible frame and stored crop origin, which keeps blocks fixed in document space and prevents the frame from snapping back after release. A click without movement exits without adding a false history entry. **Areas:** composer geometry state, authoring browser regression, README, and design review. **Verification:** the optimized frontend image builds and is running; TypeScript and the callback-comment audit pass. The browser verifies zero movement on press, retention of a 35 × 25 pixel frame move, and matching persisted x/y deltas. All eight Playwright workflows pass in 31.0 seconds.

- [x] **I26 - Verify recovered live AI retrieval.** Confirmed the project-scoped OpenAI key is active after billing propagation, allowed the durable worker to finish its queue, and exercised both cloud stages through the live application. **Verification:** all 22 cloud-eligible passages have vectors, the one non-consenting private passage remains keyword-only, and there are zero pending or failed jobs. `mountain` returns hybrid ranking with no fallback and the expected public post; the grounded explanation stream completes without an error event. The checked-in 16-case evaluation reaches 100% top-five accuracy overall and 100% across eight English/Spanish cross-language cases.

- [x] **I27 - Add pointer-centered canvas zoom.** Added a reusable composer hook for Ctrl + wheel zoom from 25% to 300% in 10% steps. A non-passive viewport listener prevents browser-page zoom, keeps the document point under the pointer stationary by restoring scroll offsets after scaling, and leaves ordinary wheel scrolling unchanged. The scaled workspace reserves its rendered dimensions, while React Rnd and drawing coordinates use the active scale so canvas and block editing remain accurate. A bilingual percentage indicator shows the current level, and zoom remains transient rather than changing saved post geometry. **Areas:** canvas zoom hook, composer workspace, composer styles, authoring browser regression, README, and design review. **Verification:** the production frontend image builds and is running; TypeScript and the callback-comment audit pass. Browser coverage verifies 100% to 110% pointer-centered zoom, return to 100%, unchanged backend geometry, click-stable canvas movement, and subsequent authoring behavior. All eight Playwright workflows pass in 35.2 seconds.

- [x] **I28 - Add the animated Discover mascot.** Generated an original adult anime cat-girl guide with white hair, red eyes, a journal, and transparent alpha, then added it as a dedicated homepage component. The hero uses a restrained floating loop, breathing aura, and rising particles; it preserves the existing Japanese-fantasy editorial palette, keeps pointer interaction clear, stacks cleanly on phones, and becomes static for visitors who prefer reduced motion. **Areas:** `frontend/public/characters/ma-cat-guide.png`, the home feature, a focused mascot stylesheet, global style registration, desktop/phone design captures, and this progress record. **Verification:** transparent corner alpha was confirmed, TypeScript and the function-comment audit pass, the optimized Next.js build passes, and the rebuilt frontend container is running. Desktop 1440 × 900 and phone 390 × 844 captures were inspected after correcting an initial top-edge crop; the final artwork remains complete and does not overlap navigation or carousel controls. The existing eight-workflow browser suite was not repeated because this change adds no behavior, data, or interactive controls.

- [x] **I29 - Revert the unapproved mascot implementation.** The user rejected the first visual direction and requested an image-first approval process, so the Discover page, global stylesheet imports, and project assets were restored to their previous state. The generated concept remains outside the project for reference, while all future images will be reviewed before any site integration. **Areas:** home feature, global styles, rejected mascot component/style/asset, temporary design captures, and this progress record. **Verification:** the rejected files and references are absent, TypeScript and the function-comment audit pass, and the restored production frontend image builds successfully. The frontend container was recreated from that restored image and is running; no new design will be applied until the user explicitly approves it.

- [x] **I30 - Add the approved rotating homepage characters.** Added the approved transparent catgirl, technical robot, and reader artwork as equal-height layers behind the Discover carousel. Two outward-facing characters appear at once and cross-fade every 3.5 seconds through catgirl/robot, robot/reader, and reader/catgirl; the timer pauses in hidden tabs and reduced-motion mode keeps the first pair static. Phone positioning keeps both character heads visible behind the center card, and implementation is separated into a component, lifecycle hook, and focused stylesheet. **Areas:** `frontend/public/characters/home-cycle/`, home character component/hook, home composition, character-cycle styles, carousel browser regression, design records, README, and desktop/phone captures. **Verification:** TypeScript and function-comment audits pass; the Docker production frontend build passes and is running. The focused character/carousel regression passes, the complete eight-workflow Playwright suite passes in 35.6 seconds, and 1440 × 900 plus 390 × 844 live captures were visually inspected.

- [x] **I31 - Apply the approved dark celestial Discover background.** Replaced the rejected black-hole study with a full-width black hero scene that places three centered gold/crimson rings and independently drifting stars behind the headline, rotating characters, live carousel cards, and controls. The light collection section remains unchanged, the SVG scales and crops around its center on phones, and reduced-motion mode removes every celestial animation. The background is isolated in its own component and focused stylesheet. **Areas:** home celestial-background component, home composition, celestial/character styles, global style registration, carousel browser regression, design records, README, and desktop/phone live captures. **Verification:** TypeScript and the function-comment audit pass; the optimized Docker frontend build passes and the rebuilt container is running. The focused homepage regression passes in 20.2 seconds, all eight Playwright workflows pass in 32.9 seconds, and live 1440-pixel desktop plus 390-pixel phone captures were inspected with no horizontal overflow.

- [x] **I32 - Equalize character proximity to the carousel.** Changed both character slots to anchor each illustration from its carousel-facing edge. The catgirl's narrower source ratio now changes only how far the image extends toward the screen edge, while her inner edge stays aligned with the wider robot and reader artwork. **Areas:** character-cycle styles, carousel regression, this progress record, and left/right/phone visual captures. **Verification:** TypeScript and the function-comment audit pass; the optimized Docker frontend build passes and the rebuilt container is running. The focused 20.1-second browser regression verifies equal height and symmetric inner-edge distance, and live 1440 × 900 captures inspected the catgirl on both sides plus the 390 × 844 phone layout.

- [x] **I33 - Preserve state and add administrator and publisher identities.** Created `backups/pre-ai-news-20260914`, applied additive migration `0004`, promoted the configured local account to a protected database administrator, and seeded the no-login `MABlog_IA` system publisher. Existing posts, uploads, search passages, and vectors remain in their persistent volumes. **Verification:** the live database reports Alembic `0004 (head)` and the publisher is active, system-owned, and not an administrator.

- [x] **I34 - Protect administrator authority and audit mutations.** Added server-side administrator authorization, password plus email-code step-up for protected actions, the local-only bootstrap bypass, role management with last-administrator protection, per-administrator notification preferences, and durable audit records. **Areas:** administrator API/services, auth/bootstrap, models/schemas, newsroom administrator component. **Verification:** backend coverage rejects regular accounts from the newsroom and the browser signs in with the local administrator without an email challenge.

- [x] **I35 - Add durable newsroom records and readiness.** Added settings, official sources and suggestions, runs, stage jobs, fetched documents, candidates, claims, editions, revisions, alerts, usage, source checks, readiness reporting, and the verified OpenAI/Gemini/Claude/DeepSeek/Kimi registry. The health endpoint now exposes actionable newsroom readiness and queue failures without secrets. **Verification:** fresh metadata creation/drop and live migration pass; Docker reports healthy database/API/email and optional Brave as degraded when unconfigured.

- [x] **I36 - Fetch and extract official evidence safely.** Added HTTPS-only validation, credential/port rejection, DNS and redirect rechecks, private-address blocking, robots handling, size/time/type limits, and bounded extraction for HTML, PDF, Markdown, and plain text. Source text is isolated as untrusted input, while canonical URL, hash, metadata, warnings, and the expiring full snapshot are retained. **Verification:** regression tests reject insecure, credentialed, private, and disallowed-port URLs; the real preview safely recorded one provider fetch rejection and continued with three official sources.

- [x] **I37 - Discover and classify meaningful model changes.** Added OpenAI and optional Brave discovery, registry-only degraded operation, release-window validation, official-source evidence, normalized lifecycle types, inactive source suggestions, and update-aware deduplication. Classification retries rebuild their checkpoint to avoid duplicate records. **Verification:** the live preview discovered five verified registry pages, identified seven qualifying lifecycle updates in its historical window, and used no Brave calls because no key is configured.

- [x] **I38 - Compose, verify, repair, and moderate bilingual editions.** Added economical Luna evidence extraction and Terra writing/independent verification with exact claim citations, equivalent English/Spanish typed documents, deterministic safety rules, OpenAI moderation, and two bounded repair attempts. A malformed repair now preserves the last complete edition and proceeds to the remaining attempt. **Verification:** a new regression covers malformed first-repair recovery; the live edition passed every claim, citation, language, quotation, secret, personal-data, markup, and moderation gate.

- [x] **I39 - Run the weekly pipeline durably.** Added a dedicated `news-worker`, PostgreSQL leases and checkpoints, 1/5/20-minute retries, one-active-run enforcement, one collapsed waiting catch-up, two-day scan overlap, idempotency, restart recovery, quiet runs, per-run/month/query budgets, and an environment kill switch. Backup now pauses both writers. **Verification:** concurrency/catch-up integration passes, and the private run resumed its failed verification checkpoint after worker rebuild without redoing completed research stages.

- [x] **I40 - Publish, correct, monitor, retain, and index editions.** Added atomic localized publication, branded covers, system-profile ownership, AI verification metadata, private corrected revisions, urgent unpublish-and-retain, source contradiction handling, 30-day monitoring, tiered evidence retention, and durable keyword/vector indexing. **Verification:** presentation tests return the selected localization and persistence tests cover the publication structures without creating a live public side effect.

- [x] **I41 - Deliver the administrator AI newsroom.** Added individual Next.js components, hooks, API transport, and Zustand state for readiness, schedule, cost limits, runs, stage detail, pin/retry, sources and suggestions, editions, bilingual corrections, alerts, email preferences, and administrator roles. Protected operations explain and request step-up when required. **Verification:** TypeScript and the callback-comment audit pass; focused desktop inspection and the administrator Playwright workflow confirm the newsroom can be reviewed and unlocked without publishing.

- [x] **I42 - Integrate verified AI news with public MAblog surfaces.** Added locale-selected structured reading, inline citations, official section sources, correction labels, search passages for both languages, the `MABlog_IA` profile, and a maximum of one AI-news card among five carousel entries. **Verification:** backend presentation coverage confirms Spanish selection and the one-card limit; all existing home, collection, reader, search, and responsive browser workflows continue to pass.

- [x] **I43 - Pass the real private release gate.** Rebuilt the frontend, backend, and news worker; exercised a safe failed verification and checkpoint retry; then completed run `bd330f2e-4f6a-40a2-bcb6-4d3cfeda2df8` as a 30-day historical preview. It reached 100%, used three official sources, passed independent bilingual verification and moderation, cost an estimated USD 0.225456, retained no publication intent, and left `post_id` empty. PostgreSQL recorded this run as the activation preview while `schedule_enabled` remains false. **Verification:** the final backend suite passes all 34 tests in 11.04 seconds and the exact rebuilt release passes all nine Playwright workflows in 40.6 seconds.

- [x] **I44 - Complete local operating guidance.** Documented provider/configuration readiness, activation, degraded discovery, stage retry, restart recovery, source curation, budget and retention tiers, correction/unpublication, backup/restore, master kill switch, and future production needs. Updated README, architecture, local operations, design, ADR, implementation plan, and this progress ledger. **Records:** `docs/ai-news-operations.md` and the linked project documents. **Verification:** documentation matches the live disabled schedule, retained activation proof, Compose services, and current environment defaults.

- [x] **I45 - Normalize structured citation display.** The real preview showed that a provider could return citation numbers both in paragraph prose and in the typed citation array. Ingestion now removes only trailing bracket markers and the public reader applies the same compatibility cleanup to retained previews, so each source number appears once as a clickable link while verified prose and evidence remain unchanged. **Areas:** bilingual composition prompt/normalizer, typed public reader, and backend regression. **Verification:** the optimized frontend/backend/news-worker images rebuild successfully; all 34 backend tests, TypeScript, and the function-comment audit pass.

- [ ] **I46 - Re-establish the local development and test environment.** **Blocked:** Docker Desktop 4.91.0 was repaired and its engine started after stale Windows AF_UNIX runtime directories were moved aside. A complete Compose build succeeded and all seven services became healthy, but verification showed that the fresh install's active WSL data disk was on `C:` rather than the requested `D:` location; the new blank stack was stopped without deleting volumes before relocation. The next privileged operation is to reinstall or reconfigure Docker with `--wsl-default-data-root=D:\DockerData`, but Codex's elevated-command allowance was exhausted until 2026-09-19, so the test database, seed users, backend PostgreSQL suite, and Playwright suite were not run. **Verification completed:** Docker Engine 29.8.0 ran successfully; Compose built the frontend, backend, migration, indexing-worker, and news-worker images; the live health response reported PostgreSQL and Redis healthy; `npm run typecheck`, `npm run lint`, `npm run audit:comments`, and `python -m compileall -q app tests` pass. Host `npm ci` remains blocked by Windows `EPERM` on the pre-existing generated `frontend/node_modules/.bin`, although the same locked install completed successfully inside the Docker frontend build.

  **Follow-up, 2026-09-16:** At the user's request, the already-built Compose stack was started again for manual inspection. All seven long-running services are up, PostgreSQL, backend, and Mailpit report healthy, `GET /api/health` returns `status: ok` with PostgreSQL and Redis available, and the home page returns HTTP 200. The active Docker WSL disk remains on `C:` pending the requested move to `D:`; no claim is made that I46 or the full test suite is complete.

  **Follow-up, 2026-09-17:** A separate local `godot_ai` process already owned host port 8000, so MAblog was started with a temporary external Compose override that maps the backend to `127.0.0.1:8001` while preserving the container's internal `backend:8000` address. All seven services are running; the frontend and API documentation return HTTP 200, and the proxied health endpoint reports healthy PostgreSQL and Redis with no pending or failed indexing work. The unrelated Godot process was left untouched.

- [x] **I47 - Restore and run the sample public-post seeder.** The explicit seed command exposed a stale compatibility import left after public-cache behavior moved out of `app.main`; `backend/app/seed.py` now imports `invalidate_public_cache` directly from its owning service. The initial command had already committed its transaction before reaching the stale import, and the corrected rerun confirmed the operation remained idempotent. **Areas:** backend sample seeder and this progress ledger. **Verification:** rebuilt the backend image, reran `python -m app.seed` successfully, queried PostgreSQL to confirm exactly five public posts owned by the `mablog_demo%` accounts, and confirmed the public API returns the same five expected titles.

- [x] **I48 - Add the production-style public footer and information routes.** Adapted the supplied spacious callout and dark multi-column footer reference to MAblog's cream, crimson, gold, and editorial type system. The global shell now includes a keyboard skip link, a discovery callout on public/general routes, truthful product-value markers instead of fictional customer logos, and navigation only to implemented destinations; focused authoring, review, sharing, and administrator routes omit the large callout. Added bilingual About, Help, Community Guidelines, Privacy, and Terms pages using one reusable information-page component, with explicit warnings that the local privacy and terms overviews still require operator-specific legal review before public deployment. **Areas:** `frontend/src/app-shell/`, `frontend/src/features/site-info/`, the five new App Router destinations, footer/information/base/responsive styles, route tests, architecture documentation, and this progress ledger. **Verification:** `npm run typecheck`, `npm run lint`, and `npm run audit:comments` pass; the host and Docker optimized production builds pass and list all five routes. The focused footer suite passed 2/2, then the complete Playwright suite passed all 12 workflows in 1.1 minutes. Live desktop visual inspection confirmed the hierarchy and final cream-on-crimson CTA color pair; the automated 375 × 812 check found no horizontal overflow. The rebuilt frontend is running at `http://localhost:3000` with the existing temporary backend mapping at `http://localhost:8001`.

- [x] **I49 - Expand the public sample collection by ten stories.** Added ten complete illustrated stories across Technology, Travel, General, and Anime, bringing the live public collection from five to fifteen posts. Refactored the explicit sample seeder so it creates missing demo accounts and stories independently by stable title instead of exiting as soon as the first demo account exists; new posts receive public metadata, media ownership, engagement fixtures, and immediate keyword/vector-index preparation, while existing records remain untouched. **Areas:** `backend/app/seed.py`, `README.md`, and this progress ledger. **Verification:** the rebuilt backend image ran `python -m app.seed` and reported exactly ten new stories; an immediate second run reported zero, proving idempotency. The public API returned fifteen posts with all ten new titles, and an independent PostgreSQL query returned exactly fifteen public records. Python compilation was also exercised inside the successful Docker image build.

- [x] **I50 - Add incremental public-post loading.** The Discover and Collection routes now server-render three posts rather than the full public catalog, then append one ordered three-post API page when a 160-pixel end sentinel approaches the viewport. A visible bilingual `Loading more posts…` status uses the collection's reserved 72-pixel boundary area, failed requests expose a 44-pixel retry control, existing cards keep their identity, and cover images use native lazy loading plus asynchronous decoding. The observer stays mounted during an append so a stationary sentinel cannot cascade from three directly to nine posts; the reader must continue scrolling to cross the next boundary. Category or language changes reset the feed and invalidate stale responses. **Areas:** bounded FastAPI `offset`/`limit`, the reusable posts pagination constant/hook, Discover and Collection server routes, home feed composition, card image behavior, post styles, backend/browser regressions, architecture documentation, and this ledger. **Verification:** optimized backend/frontend Docker builds pass and are live. TypeScript, ESLint, and the function-comment audit pass; all 35 isolated backend tests pass, including stable non-overlapping pages and invalid-limit rejection. The focused lazy-loading suite passes 2/2, and the complete 14-workflow Playwright suite passes in 59.7 seconds after updating one stale empty-Travel fixture assumption. Automated 375 × 812 validation found no horizontal overflow, and the live desktop collection was visually inspected with exactly the first three cards rendered before scrolling. Two exact browser-test records and their upload files left by earlier failed runs were removed; the sample collection remains at fifteen public posts.

- [x] **I51 - Reveal Discover posts from below.** Each Discover card now fades in while moving 28 pixels upward when it enters the reading viewport, including cards appended by incremental loading. The isolated observer disconnects after the first reveal, animates only opacity and transform to preserve grid geometry, leaves the Collection and other post grids unchanged, and immediately presents the final state when reduced motion is preferred or observation is unavailable. **Areas:** Discover feed composition, reusable post grid, dedicated reveal-card component, post motion styles, focused browser regression, and this ledger. **Verification:** TypeScript, ESLint, and the function-comment audit pass; the optimized Docker frontend build passes and the rebuilt container is live at `http://localhost:3000`. The focused reveal suite passes 2/2, checking entry, settled opacity/position, route scope, and reduced motion; the complete Playwright suite passes all 16 workflows in 1.2 minutes.

- [x] **I52 - Sequence Discover reveals from left to right.** Discover cards in each desktop row now enter in visual order with 0, 90, and 180-millisecond delays; two-column rows use 0 and 90 milliseconds, while single-column phones avoid unnecessary delay. The sequence retains the existing upward fade, one-time observation, layout stability, lazy-page coverage, and reduced-motion override. **Areas:** post motion styles, focused reveal regression, and this ledger. **Verification:** TypeScript, ESLint, and the function-comment audit pass; the optimized Docker frontend build passes and the rebuilt container is live. The focused regression verifies the exact three-card delay sequence and reduced-motion state, and the complete Playwright suite passes all 16 workflows in 49.5 seconds.

- [x] **I53 - Remove Discover hero scroll jank.** Preserved the celestial composition while removing continuous SVG repaint loops, Gaussian and drop-shadow filters, blend-mode noise, and control backdrop blur from the large hero surface. The showcase now uses paint containment and `content-visibility: auto`, and its 3.5-second character rotation pauses when the scene leaves the viewport; character and carousel transitions continue to use compositor-friendly opacity and transforms. **Areas:** celestial SVG/component styles, character-cycle visibility lifecycle, focused performance regression, and this ledger. **Verification:** TypeScript, targeted ESLint, and the function-comment audit pass; the optimized Docker frontend build passes and the rebuilt container is live. The performance regression confirms the expensive filters are absent, offscreen rotation sleeps, and the hero is render-skipped offscreen; all seven homepage regressions pass, the complete 17-workflow Playwright suite passes in 1.2 minutes, and the live desktop composition was visually inspected after rebuild.

- [x] **I54 - Allow post sharing with the local administrator.** The sharing schema now accepts a bounded registered-account address instead of applying public-domain email validation before lookup, allowing the intentionally configured `admin@mablog.local` bootstrap identity to receive ordinary per-post viewer or editor grants. The endpoint remains authoritative: it normalizes the value, requires an existing account, rejects self-sharing and invalid roles, and never stores unknown invitation addresses. **Areas:** collaboration request schema, backend workflow regression, browser collaboration regression, and this ledger. **Verification:** Python compilation, TypeScript, and the function-comment audit pass; the rebuilt backend is live. The focused backend tests pass 2/2, the complete isolated backend suite passes 36/36, the focused browser collaboration suite passes 2/2, and the complete browser suite passes all 18 workflows in 1.1 minutes, including the exact `admin@mablog.local` UI flow with test-post cleanup.

- [x] **I55 - Show the creator's pending-review count.** The post reader's Review action now carries a compact red numeric badge whenever the signed-in creator has proposal decisions awaiting approval. The count comes from the existing post response, is calculated server-side from pending proposals, decreases after each approval or rejection, and disappears at zero; collaborators keep the ordinary Review action without seeing the creator-only queue count. The complete bilingual accessible name includes the pending-review context, while the visual number is capped at `99+` without changing the real count. **Areas:** post-detail API, shared post type, dedicated pending-review link component, reader action toolbar, post styles, backend/browser regressions, and this ledger. **Verification:** the optimized backend/frontend Docker builds pass and the rebuilt services are live. TypeScript, targeted ESLint, and the function-comment audit pass; the focused backend workflow passes 1/1 and verifies count transitions, the complete backend suite passes 36/36, the focused collaboration browser suite passes 2/2, and the complete browser suite passes all 18 workflows in 1.1 minutes. Browser coverage verifies the visible red `2` badge, its accessible contextual label, the transition to `1`, and removal after all pending reviews are resolved.

- [x] **I56 - Keep the reader stable while liking a story.** Removed the post-detail reload that cleared server-rendered reader data after every like and briefly exposed the Discover-oriented loading shell. The button now updates its heart and count immediately, reconciles with the mutation response without navigating or refetching the reader, restores the previous state if the request fails, and prevents concurrent toggles while one request is pending. It also exposes bilingual accessible labels and `aria-pressed` state, with the decorative heart hidden from assistive technology. **Areas:** reader interaction, public browser regression, and this ledger. **Verification:** targeted ESLint, the function-comment audit, and non-incremental TypeScript checking pass; the optimized Docker frontend build passes and the rebuilt container is live. The focused browser regression passes and confirms zero post-detail GET requests, continuous attachment of the original composition DOM, correct count/pressed-state changes, and cleanup back to the original like state. The complete browser suite passes all 19 workflows in 1.1 minutes.

- [x] **I57 - Establish project version control.** Initialized the project as a Git repository on `main`, connected it to `https://github.com/youdianleng/MABlog.git`, and preserved the remote repository's existing initial `LICENSE` commit as the base history. Hardened the root ignore rules so the real environment file, dependency/build caches, browser binaries, test output, backups, and generated design-check artifacts stay outside version control; the distributable `.env.example`, source assets, migrations, tests, and documentation remain included. **Verification:** Git's ignored-file check confirms `.env` is excluded, the candidate-file credential scan found no private-key or common provider-token patterns, and the staged-tree audit excludes `.env`, `backups/`, `artifacts/`, dependencies, and build output.

- [x] **I58 - Add the complete heading-level menu.** Replaced the fixed H2 toolbar action with a keyboard-accessible dropdown that displays H1 by default, tracks the selected heading, and applies any semantic level from H1 through H6. Added proportional reader/editor presentation for H4–H6 and expanded backend sanitization so all six safe heading elements survive saving, submission, review, and publication. **Areas:** rich editor toolbar, composer and reader styles, document sanitization, authoring/backend regressions, design review, and this ledger. **Verification:** targeted ESLint, non-incremental TypeScript checking, and the function-comment audit pass; optimized backend/frontend Docker builds pass and the rebuilt services are live. The focused sanitization test passes, the real authoring workflow verifies the default H1 trigger, all six menu items, H4 application, and the completed publish flow; the complete backend suite passes 36/36 and the complete browser suite passes all 19 workflows in 1.1 minutes.

- [x] **I59 - Make inserted images directly resizable.** Replaced the plain TipTap image with a dedicated node view that exposes a visible lower-right resize handle, previews width continuously while dragging, and commits one bounded width transaction on release so editor undo and draft synchronization remain stable. The same focused handle supports Left/Right Arrow resizing in 10-pixel steps and Shift + Arrow in 25-pixel steps, with a bilingual accessible instruction and current-width announcement. Saved widths are restricted to 80–4000 pixels, constrained to the current editor block during interaction, preserved by backend sanitization, and rendered responsively without distorting aspect ratio. **Areas:** resizable image node, rich editor registration, composer styles, HTML sanitizer, authoring/backend regressions, README, design review, and this ledger. **Verification:** targeted ESLint, non-incremental TypeScript checking, and the function-comment audit pass; optimized backend/frontend Docker builds pass and the rebuilt services are live. The focused backend test verifies accepted and rejected widths while preserving existing XSS stripping. The real authoring workflow uploads an image, resizes it by pointer, applies an exact keyboard step, then saves and publishes; the complete backend suite passes 36/36 and the complete browser suite passes all 19 workflows in 1.1 minutes.

- [x] **I60 - Add fifteen-post page navigation.** Replaced the three-card infinite feed on both Discover and the public collection with server-rendered numbered pages containing at most fifteen posts. A shared bilingual navigation exposes previous, numbered, current, and next states with 44-pixel controls; page and collection-category state remain in the URL for refresh, sharing, and browser history, while route transitions keep the existing grid visible. The backend filters approved metadata before calculating an exact total, caps the public navigation page size at fifteen, and keeps the legacy bounded list endpoint for existing consumers. **Areas:** public-post API, shared API types and pagination component, Discover/Collection server routes and home composition, responsive styles, backend/browser regressions, README, design review, architecture, and this ledger. **Verification:** non-incremental TypeScript, ESLint, function-comment audit, and optimized backend/frontend Docker builds pass. The complete isolated backend suite passes 37/37, including 15/15/2 boundaries for 32 filtered posts and rejection of a 16-item request. The focused pagination browser suite passes 3/3 across Discover, Collection, page-two URL state, and a 375 × 812 viewport; the complete Playwright suite passes all 20 workflows in 1.4 minutes. Live API checks return 16 total public posts as pages of 15 and 1, health reports PostgreSQL and Redis ready, and the rebuilt services are running at `http://localhost:3000` and `http://localhost:8001`.

- [x] **I61 - Remove the stale-page pagination flash.** The page-change handler no longer scrolls to the collection while the outgoing cards are still mounted. Scrolling and heading focus now occur together only after the requested page and category props have replaced the old result set, preventing the first page from flashing at the top while retaining the visible pending state and accessible focus handoff. **Areas:** home collection navigation lifecycle, focused pagination regression, and this ledger. **Verification:** non-incremental TypeScript, ESLint, the function-comment audit, and the optimized frontend Docker build pass; the rebuilt frontend is live. The pagination regression records the URL at the exact `scrollIntoView` call and confirms it is already `/public?page=2`; all 3 focused pagination workflows pass, followed by all 20 Playwright workflows in 1.3 minutes.

- [x] **I62 - Show the authenticated account welcome state.** The account route now reads the application-wide session and immediately presents the requested “Welcome to your atelier” card with an “Open my workspace” link whenever a user is already signed in. The same focused component is reused immediately after successful login or verification, while anonymous visitors retain the complete sign-in, registration, verification, and recovery form. **Areas:** account page composition, reusable authenticated-account card, public authentication regression, design review, and this ledger. **Verification:** non-incremental TypeScript, ESLint, function-comment audit, and the optimized frontend Docker build pass; the rebuilt frontend is live. The focused browser regression signs in, leaves the route, revisits `/account`, confirms the credential form remains absent, and verifies the workspace link; it passes 1/1, followed by all 20 Playwright workflows in 1.3 minutes.

- [x] **I63 - Reframe About as MAblog's editorial story.** Replaced the generic information layout with a dedicated bilingual narrative page: an oversized mission hero, a focused manifesto, three asymmetric visual story cards, a dark principles panel, a four-step journey from private draft to public story, and an original crimson closing invitation. The composition borrows the supplied reference's spacious mission-led pacing without copying its layout or claims, stays inside MAblog's cream/crimson/gold visual language, uses existing optimized character artwork with declared dimensions and lazy loading, and removes the shell's duplicate generic callout from this route. **Areas:** About feature component and route, shared shell, site-information styles, focused browser regression, design review, and this ledger. **Verification:** non-incremental TypeScript, ESLint, function-comment audit, and the optimized Docker frontend build pass; the rebuilt frontend is live. Desktop visual inspection covered every page section in Spanish, while the focused About regression verifies the complete structure, CTA destinations, absence of the duplicate shell callout, and no horizontal overflow at 375 × 812. All 3 footer/About workflows pass, followed by the complete 21-workflow Playwright suite in 1.5 minutes.

- [x] **I64 - Replace the About illustration gallery with real content areas.** Removed the fantasy-character presentation that made the product feel overly anime-oriented and introduced a restrained, non-animated editorial grid grounded in MAblog's actual content: source-linked bilingual AI news, recent technology writing, and broader reader interests. The three keyboard-focusable cards use clear Lucide icons, descriptive headings, concrete coverage details, and direct collection links; the large newsroom feature and two supporting cards retain the established cream/crimson/gold palette without character imagery or decorative motion. **Areas:** About content component, site-information styles, focused About regression, design review, and this ledger. **Verification:** non-incremental TypeScript, ESLint, function-comment audit, and the optimized Docker frontend build pass; the rebuilt frontend is live. Spanish desktop inspection confirms the editorial hierarchy and both lower cards, and all 3 focused footer/About browser workflows pass against the final copy in 9.9 seconds, including the 375 × 812 no-overflow check.

- [x] **I65 - Clarify the About manifesto-to-content transition.** Removed the second oversized editorial statement that visually competed with the manifesto. A thin divider now introduces a compact bilingual “Inside MAblog” H2 beside a concise sans-serif summary, while reduced manifesto spacing brings the content grid into view with a clearer hierarchy and less empty space. The desktop structure remains two-column and collapses to a comfortably spaced single column on narrow screens. **Areas:** About content copy and hierarchy, responsive site-information styles, focused About regression, design review, and this ledger. **Verification:** non-incremental TypeScript, ESLint, function-comment audit, and the optimized Docker frontend build pass; the rebuilt frontend is live. Spanish desktop visual inspection confirms the new transition and all 3 focused footer/About browser workflows pass in 10.8 seconds, including the 375 × 812 no-overflow check.

- [x] **I66 - Create the dedicated MAblog Help experience.** Replaced the generic information page with a bilingual, route-specific help center adapted from the supplied rounded-card reference. The new warm frame contains a focused support hero, two real route actions, a code-native private-draft-to-review/publish visual, five quick-path controls, four anchored workflow guides, and a clearly separated Mailpit development note; the decorative visual is hidden from assistive technology and the route no longer repeats the shell's generic footer callout. **Areas:** dedicated Help feature and route, shell callout policy, responsive site-information styles, focused browser regression, design review, and this ledger. **Verification:** non-incremental TypeScript, ESLint, function-comment audit, and the optimized Docker frontend build pass; the rebuilt frontend is live. Spanish desktop visual inspection covered the hero, workflow graphic, topic strip, and guide introduction, while all 4 focused information/footer workflows pass in 13.3 seconds, including the dedicated Help structure and 375 × 812 no-overflow check.

- [x] **I67 - Remove the Help workflow-guide grid.** Removed the complete “Four paths cover most questions” introduction and its four large guide cards, together with their unused Lucide import and responsive styles. The retained quick-path controls now navigate to real workspace, collection, and search routes instead of missing section anchors, and the local Mailpit note follows the rounded Help frame with a compact responsive gap. **Areas:** Help feature, site-information styles, focused browser regression, design review, and this ledger. **Verification:** non-incremental TypeScript, ESLint, function-comment audit, and the optimized Docker frontend build pass; the rebuilt frontend is live. Spanish desktop accessibility and visual inspection confirm the grid is absent and the hero flows directly to the local note; all 4 focused information/footer workflows pass in 11.0 seconds, including the 375 × 812 no-overflow check.

- [x] **I68 - Give Help a distinct MAblog editorial composition.** Replaced the first reference-adjacent treatment—taupe rounded frame, white pill panel, figure-eight workflow artwork, and bottom utility strip—with an asymmetric two-column editorial desk. The dark narrative side now carries MAblog's help identity, title, actions, bilingual note, and subtle typographic watermark; the light side presents three semantic workflow steps and a bordered quick-route matrix, using sharp dividers and near-square controls instead of the reference's soft event-page language. **Areas:** Help feature structure, complete Help style system, responsive browser regression, design review, and this ledger. **Verification:** non-incremental TypeScript, ESLint, function-comment audit, and the optimized Docker frontend build pass; the rebuilt frontend is live. Spanish desktop accessibility and visual inspection confirm the distinct composition and sequential H1/H2/H3 structure; all 4 focused information/footer workflows pass in 10.7 seconds, including the 375 × 812 no-overflow check.

- [x] **I69 - Add the public AI Models field guide.** Added a dedicated bilingual `/ai-models` route with a split editorial hero, four practical model-family profiles, a transparent map of MAblog's fast/strong/embedding roles, a task-led comparison table, and a prominent reminder that provider capabilities and commercial details change. The route is linked from the shared header and footer, omits the duplicate collection callout, avoids volatile version/pricing/leaderboard claims, and collapses to a single readable column on phones. **Areas:** AI Models route and feature component, shared navigation, site-information styles, focused browser regression, README, architecture, design review, and this ledger. **Verification:** non-incremental TypeScript, ESLint, and the function-comment audit pass; the optimized Docker frontend build passes and the rebuilt route is live at `http://localhost:3000/ai-models`. Desktop visual inspection confirms the intended cream-grid/charcoal split and active navigation; the focused information suite passes 5/5, including four family cards, three MAblog roles, table semantics, disclosure copy, and 375 × 812 no-overflow coverage. The complete Playwright suite passes all 23 workflows in 1.1 minutes.

- [x] **I70 - Replace the AI field guide with reviewed model rankings.** Superseded I69 with a bilingual evidence-based edition containing independent Top 5 lists for production coding, text-to-image, text-to-video with native audio, and music; Music has separate Vocal and Instrumental views. A typed snapshot preserves source-native scores, published confidence and sample context, source rank ranges, access modes, one-family-per-provider filtering, and a 2026-09-22 evaluation date. Seven clearly labeled MAblog editorial picks and original abstract covers lead into 25 clickable placements; permanent `/ai-models/[slug]` profiles aggregate category evidence, user/developer guidance, official access, source links, and the confirmed “Full analysis coming next” state. The complete decision policy and deferred work are recorded in `docs/ai-model-rankings.md` for future ranking refreshes. **Areas:** ranking dataset, ranking and profile components, dynamic route, site-information styles, focused browser regression, README, architecture, design review, decision record, and this ledger. **Verification:** non-incremental TypeScript, ESLint, function-comment audit, and the optimized Next.js production build pass. The focused information suite passes 6/6, including 25 placements, seven editorial picks, category anchors, a two-score music profile, source/access links, and no horizontal overflow at 375 × 812 portrait or 812 × 375 landscape. Desktop visual inspection confirms the Spanish ranking hero and Mureka V9 profile. The complete 24-test browser suite reports 8 passes and 16 backend-dependent failures because Docker Desktop is stopped and `/api` returns errors; the focused public-information results remain clean, and no full-stack pass is claimed.

- [x] **I71 - Run the reviewed ranking edition with the complete local stack.** Rebuilt and restarted the production frontend container while the backend, PostgreSQL, Redis, Mailpit, and workers were running. Docker's data disk remains at `D:\Docker\WSL\disk\docker_data.vhdx`, keeping container storage on the requested drive. **Areas:** Docker Compose runtime and this ledger; no application code changed. **Verification:** `docker compose up -d --build frontend` completed, `docker compose ps` shows the frontend running and backend/database healthy, `/ai-models` and `/ai-models/mureka-v9` each return HTTP 200, and `/api/health` reports database and Redis ready. With services available, the complete Playwright suite passes all 24 workflows in 1.1 minutes, superseding I70's backend-offline verification limit.

- [x] **I72 - Align model-card details despite wrapped names.** Reserved two-line space for rank labels and model names in multi-column ranking cards, keeping provider and score rows level across all five category views while preserving natural wrapping on single-column phones. Added a desktop geometry assertion to the focused browser regression so future card-copy edits cannot quietly reintroduce this visual shift. **Areas:** AI Models CSS, focused information-page regression, and this ledger. **Verification:** the production frontend container rebuilt and restarted successfully; live inspection at 1900 px showed identical provider and score offsets for all 25 cards, and the 375 px layout had no horizontal overflow. All 6 focused Playwright tests, TypeScript typecheck, ESLint, and the function-comment audit pass; the full 24-test suite was not rerun for this CSS-only adjustment.

- [x] **I73 - Confirm the local development stack is available.** Checked the existing Docker Compose deployment rather than replacing healthy containers; the frontend, backend, PostgreSQL, Redis, Mailpit, search worker, and news worker are all running. The separate test database is also up from prior test work, while the one-shot migration service has already completed. **Areas:** local runtime check and this ledger; no application code or data changed. **Verification:** `docker compose ps` reports a healthy backend and database; `http://localhost:3000/` returns HTTP 200, and `/api/health` reports `status: ok` with database and Redis ready. The AI-news health detail notes Brave search is not configured; no tests or rebuild were needed for this status check.

- [x] **I74 - Ground AI-news benchmark results and deepen release analysis.** The classification stage now samples later benchmark-table text from bounded official documents, asks for representative exact-score claims, tags and caps source-verified provider benchmark results, and retains their original quote/citation identity. New bilingual edition blocks require distinct cited explanations of the release change, developer impact, and everyday-user impact, with limitations where supported; the separate verification prompt checks metric setup, comparisons, and unsupported benefit claims. Benchmark rows are assembled deterministically from persisted claims rather than freely rewritten by the composer, and a missing result is disclosed as unverified instead of fabricated. **Areas:** AI-news classification, composition, verification, edition tests, and editorial design record. **Verification:** the isolated backend test image passes Ruff on changed files and all 40 backend tests against the separate test database; new tests cover later-source benchmark access, exact-quote rejection, structured benchmark visibility, impact requirements, and correction locking. No paid provider call or live newsletter preview was run, so current third-party release coverage was not verified end-to-end.

- [x] **I75 - Present and deploy the richer newsletter format.** The public bilingual reader now labels release implications and shows provider-published benchmarks in a cited, responsive panel with an explicit non-independent-test warning. The compatibility post copy includes benchmark text for legacy readers and search indexing, while bilingual corrections lock the source-backed scores, citation assignments, and focus structure; previously published editions remain readable without the new panel. **Areas:** AI-news reader, API transport types, styles, compatibility publication, correction service, architecture note, and this ledger. **Verification:** frontend ESLint, TypeScript, and function-comment audit pass, the optimized Docker frontend build passes, and `docker compose up -d --build backend news-worker frontend` completed with the schedule confirmed disabled beforehand. The rebuilt homepage returns HTTP 200, `/api/health` reports database and Redis ready, and the focused AI-news browser smoke test passes 1/1. No newsletter was generated or published during deployment; a real sourced preview remains to be reviewed before judging editorial quality.

- [x] **I76 - Add protected AI-news provider configuration.** Added an additive migration to store newsroom-only OpenAI and Brave key overrides as Fernet ciphertext and fast/strong model IDs on the durable settings row. A dedicated service resolves saved values with environment fallback for both the API and worker; status responses expose only configured/source flags and model IDs, while step-up-protected mutations audit nonsecret metadata and reject changes during an active run. The encryption key is purpose-separated from `APP_SECRET`, so operators must preserve that secret across restores and replace saved overrides if it rotates. **Areas:** AI-news settings model/migration, focused provider API and service, provider adapters and pipeline call sites, schema validation, dependency pin, backend regression, and operations/design/architecture docs. **Verification:** migration `0005` applied successfully and exactly four nullable columns were confirmed; the schedule stayed disabled with no active run. Focused Ruff passes, all 41 isolated backend tests pass, including authorization, encrypted storage, redacted validation, model selection, and active-run lockout. The deployed backend reports healthy PostgreSQL/Redis and OpenAI ready; Brave remains unconfigured/degraded. No real provider key or paid model call was used.

- [x] **I77 - Expose Providers & models in the administrator newsroom.** Added a bilingual tab with separate key-rotation cards, nonsecret source badges, explicit saved-override removal, and fast/strong model fields. The form clears a successfully saved secret, keeps each provider independent, explains environment fallback and pricing-estimate limits, and uses the existing protected-action unlock with visible loading and error feedback. **Areas:** newsroom navigation, API transport types, provider panel, responsive styles, step-up copy, and focused browser regression. **Verification:** frontend ESLint passes with zero warnings, TypeScript and function-comment audit pass, and the optimized Docker frontend build succeeds. The rebuilt frontend is live at `http://localhost:3000/admin/ai-news` (HTTP 200); an unauthenticated in-app browser session correctly shows the administrator-access gate. The focused signed-in Playwright test could not run because this Windows host refused to launch the existing headless Chromium binary (`spawn UNKNOWN`), so signed-in visual interaction remains unverified in this environment; its test was added for the next browser-capable run. No key was entered through the interface and no Git commit or push was made.

- [x] **I78 - Unblock repair verification and explain failed AI-news runs.** Inspected the retained September 24 failed preview: two pricing claims had been rejected because their exact quotes omitted units or conditions, yet later verification still received those rejected records and repeatedly objected to the repaired edition's evidence scope. Verification now reads and updates only claims eligible for composition, prevents a vacuous pass with no eligible claims, and instructs the writer not to treat missing eligible evidence as proof that a product lacks a feature or price. The administrator run drawer explains what failed, displays up to three bounded saved verifier reasons as plain text, keeps the technical code available, and clarifies that missing optional Brave search does not block generation. **Areas:** AI-news composition and verification services, regression test, newsroom overview and failure panel, styles, and this ledger. **Verification:** the focused AI-news suite passes 11/11 and the complete isolated backend suite passes 42/42; changed-file Ruff, frontend TypeScript, ESLint, function-comment audit, and optimized Docker build pass. Rebuilt backend, news worker, and frontend are live; `/admin/ai-news` returns HTTP 200 and `/api/health` reports healthy PostgreSQL/Redis. The schedule remains disabled and the historical failed run was not retried or modified. No paid live preview or signed-in browser interaction was performed, so a new end-to-end provider generation remains to be checked by an administrator; existing test dependencies emit two deprecation warnings but no test errors.

- [x] **I79 - Add an audited evidence-only publication exception.** A failed private preview can now be published manually from its execution drawer only when the failure is specifically evidence verification: the protected action requires administrator step-up, an explicit acknowledgement and written reason, a complete bilingual draft, unchanged retained official sources, no duplicate release, and fresh deterministic safety and provider moderation. The failed fact-check remains recorded, the exception does not activate or advance scheduled publishing, and public reader, collection-card, and carousel labels disclose the exception instead of claiming verification; a later fully verified correction can restore the verified badge while retaining the override history. The inline confirmation follows the site's editorial styling, provides keyboard focus and narrow-screen behavior, and leaves publication entirely to the administrator. **Areas:** AI-news dashboard schema/API, publication and monitoring services, correction acceptance, public post metadata, newsroom confirmation and failure detail, reader/card/carousel disclosures, responsive styles, operations/design/architecture docs, backend/browser regressions, and this ledger. **Verification:** isolated backend suite passes 44/44 (two dependency deprecation warnings), frontend TypeScript, ESLint, function-comment audit, and optimized Docker build pass. The complete browser suite passes 25/25, including the new confirmation, card alignment, and data-independent pagination checks; after the final carousel disclosure rebuild, focused newsroom/public browser tests pass 9/9. Backend/news-worker/frontend remain deployed locally; no evidence exception was actually submitted, no real provider moderation request was made for this feature, and no Git commit or push was made pending user confirmation.

- [x] **I80 - Permit changed official pages for a deliberate evidence exception.** Supersedes only I79's unchanged-source requirement for its administrator exception: a reachable official page whose live content hash differs from the retained snapshot is now recorded with both fingerprints instead of blocking publication. Normal verified-preview publication still rejects changed sources, while unreachable sources, duplicate releases, malformed bilingual drafts, and unsafe content still block the exception. The confirmation now explicitly includes source-change risk, and a published exception exposes a reader warning when a page changed before approval; the saved snapshot remains the article's evidence and subsequent source-contradiction monitoring still applies. **Areas:** AI-news dashboard source recheck, public post metadata and reader, administrator confirmation, backend/browser regressions, operations/design/architecture documentation, and this ledger. **Verification:** the rebuilt isolated backend suite passes 44/44 with two existing dependency deprecation warnings, including changed-source success and normal-publish, unreachable-source, and unsafe-content rejection; changed-file Ruff, frontend TypeScript, ESLint, function-comment audit, and optimized Docker backend/frontend builds pass. The rebuilt local frontend/backend are running and focused administrator/public browser tests pass 9/9. No live preview was published, no paid moderation call was made for this change, and no Git commit or push was made pending user confirmation.

- [x] **I81 - Skip generated-claim fact-checking for ordinary administrator previews only.** New ordinary previews now move from bilingual composition directly to safety and remain private as `safety_cleared_preview`; scheduled/catch-up and immediate-publication runs still execute the independent verifier. A separate full-path **Test weekly pipeline** preview is required to qualify schedule activation. Publishing an ordinary preview requires administrator step-up, a written reason and explicit acknowledgement, complete bilingual structure, reachable official sources, duplicate protection, and fresh safety/moderation; it does not advance the scan cursor or activation proof. Public reader/card/carousel badges distinguish “not fact-checked” from a historical failed-verifier exception, and passing later corrections retain the earlier manual approval in private history. Existing failed legacy preview retries keep their historical verifier. **Areas:** AI-news scheduler/state machine, protected dashboard and publication service, edition correction history, public post metadata, administrator and reader UI, styles, tests, operations/design/architecture docs. **Verification:** a consistent PostgreSQL/uploads backup was saved at `backups/20260924-225354` before deployment; targeted and final complete isolated backend suites pass (final 47/47, with two dependency deprecation warnings), targeted Ruff, frontend TypeScript, ESLint, and function-comment audit pass, optimized Docker build succeeds, and all 26 browser workflows pass. Backend, news worker, and frontend are rebuilt and running; `/api/health` reports healthy PostgreSQL/Redis and zero pending indexing jobs, while the weekly schedule remains disabled. No live newsletter was generated or published, no paid moderation request was made for this change, and no Git commit or push was made pending user confirmation.

- [x] **I82 - Make verified-preview publication failures actionable.** The **Publish preview** action had discarded rejected API promises, making an older Claude Opus 5.5 preview appear inert. Read-only diagnosis found that release was already public in a September 23 edition and that at least one retained official source had changed; the newer GPT-6 preview had no duplicate or source-recheck conflict at diagnosis time. Run detail now identifies previously published releases and links their existing public posts, disables duplicate publication, and checks duplicates before source drift so the primary blocker is explained. Verified-preview publication also shows an inline API error or explicit success state instead of silently doing nothing. The safety, step-up, duplicate, and source-integrity publication gates remain in place; no user content was republished or bypassed. **Areas:** AI-news dashboard run detail/source recheck, newsroom drawer/types/styles, backend and browser regressions, operations/architecture docs, and this ledger. **Verification:** focused AI-news tests pass 17/17, final complete isolated backend suite passes 48/48 with two existing dependency deprecation warnings, targeted Ruff, frontend TypeScript, ESLint, and function-comment audit pass, optimized backend/frontend Docker builds succeed, and all 27 browser workflows pass. The deployed backend reports the older preview's existing post link and no duplicate conflict for the newer preview; no live publish action was submitted, the schedule remains disabled, and no Git commit or push was made pending user confirmation.
